# Supabase Configuration
# Get these from your Supabase project: Settings -> API
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# Gemini API Configuration (used for "Scan Receipt with AI")
# Get a key from https://aistudio.google.com/apikey
# NOTE: this key ships inside the built frontend bundle and is visible to
# anyone who inspects the site. Set usage limits/quotas on the key in
# Google AI Studio to control exposure — see the app's Settings notes.
VITE_GEMINI_API_KEY=""
